# DAA-PRACTICAL-3
This project contains a simple implementation of the Max-Heap Sort algorithm using Python. It takes an array as input from the user and sorts the elements using a Max-Heap. The program also shows the sorted output along with the time and space complexity. It is useful for understanding how heap sort works step by step.
